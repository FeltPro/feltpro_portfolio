import "./productList.css";
import Product from "../product/Product";
import { products } from "../../data";

const ProductList = () => {
  return (
    <div className="pl">
      <div className="pl-texts">
        <h1 className="pl-title">Recent Projects</h1>
        <p className="pl-desc">
          The projects below are made using a combination of HTML, CSS and JavaScript. 
          I have recently started using React to design my websites. I have included my first portfolio site which was made without React. 
        </p>
      </div>
      <div className="pl-list">
        {products.map((item) => (
          <Product key={item.id} img={item.img} link={item.link} />
        ))}
      </div>
    </div>
  );
};

export default ProductList;
